# Agent-Crypto @erith.IA — AUDIT PASS 28

Date: 2026-08-29
Nature: COORDINATION ONLY / NO RUNTIME BUILD / NO AUTO-UPDATE WRITE

## 0. Authorities verified

Checkpoint: Pass27 commit `6c055399c945c8fcdf9f1754f7dadabe42049373`.

Repository HEAD immediately before Pass28 writes: `d8e48fd0cae3fa8e58dd5e9a2f6c0f41dc097229`, commit `agent-crypto: update Book mirror sha256:62f2f5de770`. This is not an Administrator runtime publication.

Administrator authority remains:
- build `40.4.99`;
- runtime commit `8a72f5b5f39345c86230bcdf362ce28c7345c83e`;
- release `ATLAS COLD ROUTER · PRECOMPILED HOT SHELL · PARSER DEBT RETIREMENT LOCK`;
- status `candidate_atlas_cold_router_operator_validation_required`;
- Market Core `38.15.11` protected.

No post-40.4.99 canonical operator-acceptance artifact was established by the authority inspected in this pass. This does not assert that no human test occurred elsewhere.

## 1. Pass27 conclusions retained

40.4.99 retired the old global Atlas `Element.prototype.insertAdjacentHTML` preprocessing interception and introduced a precompiled HOT shell plus cold body owner.

It did not close the canonical navigation hard-fail for cold `#auto-reader`, `#shared-memory`, `#github-memory`.

Pass25/26 executable staging remains FROZEN / STATICALLY REJECTED / DO NOT INTEGRATE.

## 2. Canonical ordering problem

Pass20 established the historical route transaction:

manifest/id normalization -> DOM target resolution -> Intermediate/mode gate -> Essential auth/pending hash -> authorized Advanced transition -> visibility/details/picker/persistence/hash/scroll.

For cold memory targets, DOM resolution happens too early. Pass25 attempted to hydrate at this early failure point, but that opened the owner before mode/auth and was therefore rejected in Pass26.

A safe successor must not add a second router or copy authorization logic outside the canonical owner.

## 3. Non-live successor router contract published

Published separately:

`2026-08-29_AGENT_CRYPTO_ATLAS_ROUTER_SUCCESSOR_CONTRACT_PASS28.md`

Required shape inside the same canonical route owner:

1. normalize id / resolve manifest;
2. apply Intermediate/mode gate using manifest/id;
3. apply the existing Essential auth + pending-hash gate BEFORE hydration;
4. after authorized transition, resolve DOM target;
5. target present -> historical synchronous commit transaction;
6. unknown missing -> historical false;
7. exactly three known lazy ids missing -> owner handoff;
8. terminal success -> re-resolve target + generation/hash guard + existing commit path.

The historical boolean synchronous API must remain intact for materialized/non-lazy targets. No global Promise conversion.

## 4. Exact terminal owner problem revalidated

`atlas-peripheral-lazy.js` 40.4.99 remains the exact hydration owner. For the three relevant targets it owns `sourcePromise`, private `hydrate(key)`, the disclosure toggle and source parsing.

Success emits `erith:presentation-resident {family:"atlas",key,build:"40.4.99"}`.

Failure sets error dataset/UI and returns false, with no terminal failure event in the exact owner inspected.

Repository-wide search results for generic presentation/hydration error channels are incomplete; no repo-wide absence claim is made.

## 5. Minimal owner terminal event contract published

Published separately:

`2026-08-29_AGENT_CRYPTO_ATLAS_OWNER_TERMINAL_EVENT_CONTRACT_PASS28.md`

Preferred separately-approved owner extension:

`erith:presentation-residency-error {family:"atlas",key,build,phase:"hydrate"}`

The event is emitted by the existing hydration owner at actual terminal failure. It exposes no raw exception as cross-module contract.

Success/failure listeners are installed before opening the existing disclosure. Exactly one matching terminal signal cleans the route pending state.

This removes the need for arbitrary timeout correctness, polling, MutationObserver or retry.

## 6. Budget proof

The proposed two-part successor requires no new business fetch, WebSocket, timer/interval, observer, storage schema/writer, scheduler, retry, source parser, public hydrate API, second hashchange handler, second Command Center handler or second picker handler.

The owner extension adds only a terminal event dispatch where the real hydration Promise is already known.

Sticky rejected `sourcePromise` behavior remains owner policy; router must not invent retry/network policy.

## 7. Why no executable staging in Pass28

The owner-terminal extension changes the previously locked `no-owner-change` scope. Although technically minimal and budget-neutral, it must be treated as a separately approved scope before executable coordination staging exists.

Therefore Pass28 remains design-only.

No runtime file, load graph, manifest, version, hash/token or `auto_update/request.json` was modified/created.

## 8. OPEN / CLOSED

CLOSED / materially reduced:
- Atlas global prototype interception debt remains RETIRED in 40.4.99.
- Oracle duplicate owner remains CLOSED unless new evidence.
- G-01/V-01 historical closures remain intact.

OPEN:
- Firefox/operator acceptance 40.4.99.
- Atlas canonical cold navigation router.
- same-owner manifest/mode/auth preflight-before-DOM refactor.
- separately-approved minimal owner terminal failure event contract.
- proof of all current boolean callers / pending-auth resume semantics against the split.
- Atlas generic residency retirement only after router proof.
- no-local-producer / Ryzen OFFLINE-N/A.
- shared monolith.
- Learning autonomous boundary/source optional architecture debt.
- Backend/Source Intelligence watch.
- Web Classic separate.

## 9. PASS 29 PRIORITY

If 40.4.99 remains current and no real operator evidence supersedes this checkpoint:

1. verify HEAD/runtime first;
2. enumerate/audit all current callers of `atlasV2OpenAdvancedForTarget()` and immediate boolean assumptions: module picker, advanced anchor delegation, hashchange/direct hash, auth pending-hash resume;
3. prove a preflight/commit split can preserve each caller without duplicate authorization or double persistence/hash/scroll;
4. audit proposed `erith:presentation-residency-error` name/lifecycle for collisions and prove owner extension budget-neutral;
5. only after these static gates pass, create an isolated coordination staging against 40.4.99; otherwise remain design-only;
6. no Administrator Build/package until static PASS plus Firefox/operator PASS.
