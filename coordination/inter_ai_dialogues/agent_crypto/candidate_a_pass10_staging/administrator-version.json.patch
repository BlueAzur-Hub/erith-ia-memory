--- public/agent_crypto_erith_ia/administrator/administrator-version.json@40.4.88
+++ CANDIDATE_A_STAGING/administrator-version.json
@@
# DEFERRED BY DESIGN — NO RUNTIME BUILD NUMBER IS INVENTED IN PASS 10.
# At build authorization only:
# 1. set actual build / global_versioning / release / status / prepared_at;
# 2. record Projects/Operations generic owner retirement without claiming file deletion;
# 3. record System generic residency reduction to Simulation only;
# 4. record Residency Audit VNext as demand-only/read-only;
# 5. preserve protected_base.market_core_modified == false and Window Manager/runtime protections;
# 6. keep legacy filenames as forensic/tombstone inventory where the manifest schema requires them.
# Pass 10 staging intentionally leaves production administrator-version.json bytes unchanged.
