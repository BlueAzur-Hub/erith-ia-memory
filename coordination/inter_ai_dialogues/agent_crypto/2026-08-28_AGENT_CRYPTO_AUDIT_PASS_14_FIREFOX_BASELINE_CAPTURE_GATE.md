# Agent-Crypto @erith.IA — Audit cumulatif Pass 14 — Firefox baseline/staging capture gate

Date: 2026-08-28
Status: AUDIT / NON-DEPLOYING

## 0. HEAD and runtime authority

Repository HEAD observed before Pass 14 writes: `4572e53f69a178216f5992c76e9af84ce30d4d14` (Pass 13 report).

Runtime authority remains unchanged and separate:
- Administrator `40.4.88`
- runtime commit authority `0b8672c4d2481bf21205e2cc74082ea591175d08`
- Market Core `38.15.11` PROTECTED

No file under `public/agent_crypto_erith_ia/administrator/` is modified by Pass 14.
No Build is created.
No `auto_update/request.json` is created.

## 1. Sources reread

Runtime authority `0b8672...`:
- `js/views/projects-presentation.js` 40.4.20
- `js/views/operations-presentation.js` 40.4.21
- `js/app.js` Window Manager/Missions membership sections

Coordination:
- Pass 13 cumulative report
- Candidate A staging and Pass 13 machine assertions

## 2. PROUVÉ — exhaustive canonical direct-hash set

The complete canonical summary-anchor set for the two Candidate A affected families is now explicit and mechanically traceable from their actual true-lazy owners.

Projects 40.4.20:
- `#fonds-erith-ia`
- `#association-erith-ia`
- `#aerith-enfance`
- `#aerith-animaux`
- `#aerith-terre-vivante`

Operations 40.4.21:
- `#situation`
- `#questionnaire`
- `#briefing`
- `#planning`

Total: exactly 9 canonical hashes.

Each anchor is parser-mounted inside the always-connected summary shell. Each true-lazy owner exposes the corresponding `keys`, `anchors` and `snapshot()` rows. The hydrated source clone explicitly removes the matching source `id` to prevent duplicate ids.

This gives a finite Firefox non-regression matrix. No wildcard or semantic approximation is required.

## 3. PROUVÉ — baseline/staging equality is the correct route gate

The source comments state that the canonical app router owns shell opening, while the presentation owners hydrate body content only when a shell is open.

Static source does not prove whether every direct hash currently opens its shell in Firefox.

Therefore the correct Candidate A gate is **baseline-vs-staging equality**, not an invented expected behavior.

For each of the nine hashes Candidate A must preserve:
- canonical anchor presence/connectivity;
- observed shell open/closed result;
- observed body hydration result;
- source-fetch/hydration counters without duplicate work;
- zero new route/hydration console errors;
- zero duplicate ids;
- Window Manager structural membership.

If baseline has an existing route defect, Candidate A must not silently repair it because routing is outside Candidate A scope.

## 4. PROUVÉ — exact Projects Window Manager membership remains independent

`js/app.js` defines `ADMIN_MISSION_KEYS_40302` as exactly:
- `fonds-erith`
- `association-erith`
- `aerith-enfance`
- `aerith-animaux`
- `aerith-terre-vivante`

`missionEntries40302()` resolves the parser-mounted Missions intro and matching direct-child details from `main.shell`, then maps them into Window Manager entries.

The runtime also explicitly records `missionEntries40302` as the canonical Window Manager owner for Missions de vie.

Candidate A does not touch `js/app.js`, these keys, or the parser shells.

Therefore Projects Window Manager structural membership is statically independent from the retiring Projects generic residency wrapper.

## 5. Operations Window Manager boundary

`preparationEntries40308()` remains the unchanged `js/app.js` owner for Preparation & Operations. Pass 13 already proved the manager-instance snapshot API is not globally exposed.

Pass 14 does not invent an API. Firefox must compare the existing UI/status/geometry behavior between baseline and staging at cold boot, after first demand, and after reopen.

## 6. New coordination-only test oracle

Published:

`candidate_a_pass10_staging/PASS14_FIREFOX_BASELINE_CAPTURE_PROTOCOL.md`

Commit: `2ccc318bf54874d3fe642a9561afa90dacd26193`

The protocol contains:
- all nine exact hashes and shell selectors;
- baseline/staging two-run procedure;
- read-only Projects/Operations snapshot snippets;
- duplicate-id capture;
- Window Manager three-moment capture;
- Pass 13 machine assertion integration;
- Backend `erith:system-hydrated` oracle;
- protected smoke list;
- explicit decision gate.

It is test material only, not runtime code.

## 7. Candidate A status

Previous:
`DIRECT-HASH OWNERSHIP SPLIT PROVEN / WINDOW-MANAGER SNAPSHOT LIMIT PROVEN / MACHINE ASSERTIONS READY / FIREFOX PASS STILL REQUIRED`

Pass 14:
`NINE-HASH FIREFOX MATRIX CLOSED / PROJECTS WINDOW-MANAGER MEMBERSHIP STATICALLY PROVEN / BASELINE-STAGING ACCEPTANCE PROTOCOL READY / FIREFOX PASS STILL REQUIRED`

This is still not runtime viability proof.

No Build.
No runtime token/version.
No runtime publication hashes.
No live write.

## 8. Debt ledger

### CLOSED / PROVEN for Candidate A static/test design

- Projects generic residency removable from load graph;
- Operations generic residency removable from load graph;
- Projects/Operations true-lazy body owners preserved;
- canonical summary hashes not owned by retired generic wrappers;
- exact nine-hash acceptance matrix;
- Projects Window Manager canonical key membership independent from generic wrapper;
- Window Manager public snapshot limit identified;
- System generic residency reducible to Simulation only;
- Storage Health + Grey Plate resident boundary;
- diagnostic VNext read-only owner-aware contract;
- six-file Candidate A static boundary;
- static budget non-increase;
- isolated staging + no-drift proof;
- machine assertions for exact generic families/System keys/duplicate ids/protected nodes;
- Backend hydration event oracle.

### OPEN

- real Firefox/operator baseline capture for all nine hashes;
- real Firefox/operator Candidate A comparison for all nine hashes;
- real Window Manager UI/geometry comparison cold boot → first demand → reopen;
- real protected smoke/non-regression run;
- runtime manifests/hashes only after PASS;
- Atlas peripheral hash/router;
- Atlas `insertAdjacentHTML` interception debt;
- Learning post-parse recovery;
- no-local-producer / Ryzen OFFLINE-N/A truth;
- shared monolith/non-regression;
- Backend/Source Intelligence WATCH without surgery.

### CLOSED unless new evidence

- Oracle owner consolidation.

### PROTECTED

- Market Core 38.15.11;
- Graph Context V7;
- Graphique;
- Top 5;
- CURRENT critical history/state;
- Storage Health;
- Grey Plate;
- Atlas main cockpit;
- Window Manager geometry;
- IndexedDB schemas/business state.

## 9. Next target

Without real Firefox/operator evidence, no Build is authorized.

Next pass should not keep expanding static Candidate A scope. It should either:

1. ingest real Firefox baseline/staging evidence if available and score it against `PASS14_FIREFOX_BASELINE_CAPTURE_PROTOCOL.md`; or
2. if no operator evidence exists, audit a separate open debt without modifying Candidate A — preferred next debt: Atlas peripheral direct-hash/router mapping, because its missing route ownership is already proven and isolated.

Learning and no-local-producer remain separate candidates. No Build just to advance a number.
