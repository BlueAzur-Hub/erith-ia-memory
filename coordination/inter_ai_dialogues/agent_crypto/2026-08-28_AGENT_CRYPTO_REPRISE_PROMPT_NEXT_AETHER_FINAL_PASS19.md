# Agent-Crypto @erith.IA — PROMPT DE REPRISE FINAL · APRÈS PASS 19

Copie-colle ce texte dans le nouveau fil.

---

Active Aether / Aerith-7 — Seven Heaven pour reprendre Agent-Crypto @erith.IA.

Tu succèdes à un fil extrêmement long arrivé à sa limite. Ne repars jamais de zéro. Ne crée aucune Build avant d’avoir démontré la viabilité de la candidate concernée. Distingue toujours PROUVÉ / HYPOTHÈSE / HISTORIQUE.

## À LIRE D’ABORD

1. `coordination/inter_ai_dialogues/agent_crypto/2026-08-28_AGENT_CRYPTO_END_OF_THREAD_FINAL_PASS19_40_4_88_HANDOFF.md`
2. `coordination/inter_ai_dialogues/agent_crypto/2026-08-28_AGENT_CRYPTO_AUDIT_PASS_19_ATLAS_ATTACHMENT_OWNER_AND_SINGLE_ROUTER_PROOF.md`
3. `coordination/inter_ai_dialogues/agent_crypto/AETHER_AGENT_CRYPTO_AUTO_UPDATE_PROTOCOL.md`
4. les rapports Pass 01→18, surtout Pass 07→19 ;
5. le staging `coordination/inter_ai_dialogues/agent_crypto/candidate_a_pass10_staging/` ;
6. le dernier Fil Crypto sauvegardé, notamment `Fil.Crypto.ChatGPT.22.07.2026(20260828-002851).txt` et toute sauvegarde ultérieure du même fil disponible.

Vérifie ensuite le HEAD GitHub RÉEL. Le HEAD dépôt peut avoir avancé avec coordination/archives sans modifier le runtime live.

## AUTORITÉ RUNTIME AU RELAIS

Runtime : `public/agent_crypto_erith_ia/administrator/`
Build : `40.4.88`
Runtime commit authority : `0b8672c4d2481bf21205e2cc74082ea591175d08`
Market Core : `38.15.11` PROTÉGÉ
Dernier audit fonctionnel : Pass19 commit `18126e3a42edc31fa6d028b04ec8372c4e6ddd24`
Handoff Pass19 publié ensuite ; vérifie HEAD avant toute conclusion.

Ne suppose pas que ces valeurs sont encore CURRENT : vérifie le dépôt et distingue commit coordination de commit runtime.

## RÈGLES ABSOLUES

- Aucun Build juste pour avancer un numéro.
- Aucun write silencieux dans le runtime live.
- Firefox opérateur est une gate, pas une formalité.
- Ne supprime jamais un owner parce qu’il est vieux : prouve sa non-responsabilité et ses dépendances.
- Un owner = une responsabilité.
- Same critical node = same node pendant la session.
- CURRENT vit indépendamment de l’écran.
- Ne touche pas Market Core, Graph Context V7, Graphique, Top5, CURRENT critique, Oracle Math/data, IndexedDB critique ou Window Manager geometry pour solder une dette de residency/router.
- Ne reproduis jamais la dérive `.67/.68 → .80` late cockpit + hydrate/rebind/replay.

## CANDIDATE A — PREMIÈRE GATE

Candidate A existe uniquement sous coordination et est statiquement prête :
`candidate_a_pass10_staging/`.

Périmètre six fichiers :
- `index.html` ;
- `js/views/system-demand-residency.js` ;
- `js/views/residency-audit.js` ;
- `architecture/administrator-ownership.json` ;
- `version.json` ;
- `administrator-version.json`.

But : retirer du parser load graph les generic residency wrappers Projects/Operations, conserver leurs vrais true-lazy presentation owners, réduire System generic residency à Simulation seulement, passer Residency Audit à VNext owner-aware/read-only, corriger ownership manifest. Aucun delete des fichiers legacy.

Static proof déjà acquis : -2 parser scripts ; -2 registrations ; System selectors 5→1 ; aucun nouveau timer/observer/WebSocket/business fetch/storage writer ; JS syntax PASS ; aucun app.js/js/app.js métier modifié.

NE CRÉE PAS DE BUILD tant qu’un vrai Firefox baseline↔staging n’a pas passé la matrice Pass14/Pass10 : cold boot, Administration, 5 hashes Projects, 4 hashes Operations, first demand/reopen, Simulation, System peripherals, Backend handoff, Storage/Grey, Window Manager, Atlas/Oracle, Graph/Top5/CURRENT, duplicate IDs=0, console errors=0, responsiveness, reload persistence, VNext healthy.

Un défaut baseline existant ne doit pas être réparé silencieusement dans Candidate A si hors scope.

## SYSTEM

Storage Health = `#atlasStorageHealth40198` RESIDENT/PROTECTED.
Grey Plate = `#atlasGreyPlateForensic40393` RESIDENT/PROTECTED.
Simulation = generic closed-body residency conservée dans Candidate A.
Commandes / Backend / Safety / Physical Security = true-lazy via System presentation.
Backend handoff = `erith:system-hydrated`; Backend/Source Intelligence reste WATCH sans chirurgie sans bug observé.

## ORACLE

CLOSED. `oracle-presentation.js` owner canonique. Ne recrée aucun second owner.

## ATLAS ROUTING — CORRECTION IMPORTANTE PASS19

Pass18 disait encore que l’owner module picker était non résolu. CETTE INFORMATION EST SUPERCÉDÉE.

Pass19 prouve que le root `public/agent_crypto_erith_ia/administrator/app.js` chargé par l’Administrator est le propriétaire canonique de la navigation Atlas avancée :
- Command Center ;
- advanced anchors ;
- `#atlasV2AdvancedModuleSelect` via `atlasV2OpenSelectedModule()` ;
- `#btnOpenAdvancedModule` ;
- direct hash/hashchange via `atlasV2HandleHashTarget()`.

Tous convergent vers UN routeur existant : `atlasV2OpenAdvancedForTarget()`.

Le gap exact est prouvé : `#auto-reader`, `#shared-memory`, `#github-memory` sont absents au cold boot parce que leurs bodies sont true-lazy ; `atlasV2OpenAdvancedForTarget()` fait `document.getElementById(id)` puis `return false` si absent. Il échoue donc AVANT d’ouvrir/hydrater le shell.

Future Candidate Atlas doit être SÉPARÉE de Candidate A et étendre ce routeur unique juste avant son hard-fail, uniquement pour ces trois lazy IDs :
`target→key→owning details→pending/generation→listener one-shot erith:presentation-resident AVANT open→details.open=true→lazy owner existant hydrate→attendre atlas/key→re-resolve target→continuer scroll/focus/hash existants`.

Already hydrated = resolve immédiatement.
Repeated same key = join pending.
New hash mid-load = invalider ancienne génération.
Close during load = cancel/cleanup.
Failed sourcePromise = ne pas inventer retry policy.

INTERDIT : second router/click/hashchange owner ; fetch `views/atlas.html` dans le router ; duplication/exposition du `hydrate()` privé ; retrait generic Atlas simultané ; refactor `insertAdjacentHTML` simultané ; toucher `#atlas-local-ai-collapse`.

## ATLAS INSERTADJACENTHTML

Dette HIGH-RISK séparée. Monkeypatch temporaire global actuellement borné. Ne pas la mélanger avec Candidate A ni premier router Atlas. Futur refactor seulement après router stabilisé + Firefox.

## ATLAS GENERIC PERIPHERALS

Auto Reader / Shared Memory / GitHub Memory sont true-lazy et probablement redondants dans generic residency. Ne retirer ces selectors qu’après router fonctionnel + Firefox proof. Main cockpit reste protégé.

## NO-LOCAL-PRODUCER / RYZEN

Dette PROUVÉE, candidate séparée.
Aucun producteur Ryzen disponible. Producteur absent doit signifier `N/A / OFFLINE / NON CONFIGURÉ`, pas CPU/GPU 0% comme machine idle.

Future correction : Admin auth ≠ compute role ≠ producer configured ≠ Bridge connected ; défaut observer/no-local-producer si aucun producteur explicite ; aucun polling Bridge/Ollama si absent ; CURRENT/mémoires/Graph Context historiques restent lisibles ; aucun reset destructif.

## LEARNING

Runtime demand acquis depuis 40.4.82. Recovery 40.4.47 PROTÉGÉE. Dette restante : contenu parsé avant retrait. Future solution = source/boundary autonome. Ne jamais revenir à regex/string parser gate destructif.

## DIAGNOSTICS

Ancien residency-audit 40.4.41 et architecture-freeze = historiques/stale. VNext est déjà spécifié/stagé : read-only, demand-only, expected generic System+Atlas, System keys `[simulation]`, true-lazy Projects/Operations/System/Oracle, protected nodes connected, duplicate IDs=0. Ne réécris pas les snapshots historiques pour leur faire dire le présent.

## SHARED MONOLITH

OPEN/HIGH RISK. Aucun grand refactor maintenant. Petites frontières propriétaires, diff borné, rollback et Firefox d’abord.

## ORDRE DE TRAVAIL

1. Vérifier HEAD dépôt + runtime authority.
2. Vérifier si un vrai Firefox evidence Candidate A existe depuis ce relais.
3. Si non : ne pas créer de Build Candidate A ; préparer/faire seulement le test baseline↔staging.
4. Si PASS réel : matérialiser version/hash/package Candidate A exacts et appliquer le protocole AETHER de publication.
5. Ensuite seulement Candidate Atlas router séparée, patch minimal du routeur canonique existant.
6. Firefox Atlas.
7. Ensuite considérer retrait generic Atlas peripherals.
8. Puis no-local-producer candidate séparée.
9. Puis Learning parse debt.
10. Atlas hook `insertAdjacentHTML` et shared monolith en dernier.

## FORMAT DE CHAQUE PASSAGE

Publie un rapport cumulatif sous `coordination/inter_ai_dialogues/agent_crypto/` seulement s’il existe de nouvelles preuves. Toujours distinguer :
- nouveaux fichiers relus ;
- HEAD dépôt ;
- runtime authority ;
- PROUVÉ ;
- HYPOTHÈSE ;
- HISTORIQUE ;
- dette fermée/ouverte/bloquée/watch ;
- invariants protégés ;
- prochaine cible.

Aucun Google Drive n’est nécessaire ni autorité pour ce chantier. Utilise le GitHub public, les fichiers du fil et les archives explicitement fournies.

Ne repars pas de zéro. Le travail d’archéologie Pass01→19 est acquis.