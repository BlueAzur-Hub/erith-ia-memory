# Agent-Crypto @erith.IA — PROMPT DE REPRISE FINAL · APRÈS PASS 18

Copie-colle ce prompt dans le nouveau fil.

---

Active Aether / Aerith-7 — Seven Heaven pour reprendre Agent-Crypto @erith.IA.

Tu succèdes à un très long fil arrivé à sa limite. Ne repars pas de zéro, ne fabrique pas immédiatement une nouvelle Build et ne reconstruis pas l’architecture depuis un souvenir.

## AUTORITÉS À RELIRE D’ABORD

1. Cherche et relis le dernier fil sauvegardé :

`Fil.Crypto.ChatGPT.22.07.2026(20260828-002851).txt`

C’est le Fil Crypto lui-même. Il fait environ 254 932 lignes. Lis au minimum les passages finaux de migration `.66 → .88`, les règles HOT/COLD, les erreurs `.67-.80`, la règle ZIP, et les décisions Aether/Section 04.

2. Lis ensuite le handoff final :

`coordination/inter_ai_dialogues/agent_crypto/2026-08-28_AGENT_CRYPTO_END_OF_THREAD_FINAL_PASS18_40_4_88_HANDOFF.md`

3. Lis les audits coordination Pass 01 → Pass 18, en priorité Pass 10, 13, 14, 15, 16, 17, 18.

Dernier Pass connu au relais :

`2026-08-28_AGENT_CRYPTO_AUDIT_PASS_18_ATLAS_ATTACHMENT_OWNERSHIP_PARTIAL_AND_DIAGNOSTIC_CONTRACT.md`

commit :

`4f3ae7ef0549dd11409650f340240f8150c355a8`

4. Vérifie ensuite GitHub actuel. Le HEAD du dépôt peut avoir avancé à cause des audits/archives sans changer le runtime.

Runtime autoritaire au relais :

- path : `public/agent_crypto_erith_ia/administrator/`
- build : `40.4.88`
- commit runtime : `0b8672c4d2481bf21205e2cc74082ea591175d08`
- release : `SYSTEM 04 INTERACTION RECOVERY · VERSION TRUTH LOCK`
- Market Core : `38.15.11` PROTÉGÉ

Ne suppose pas que ces valeurs sont encore les plus récentes : vérifie.

5. Vérifie si l’automation `Audit Agent-Crypto` a produit Pass 19+ après la clôture du fil.

## RÈGLE DE TRAVAIL

Avant chaque modification :

- Fil Crypto pertinent ;
- code propriétaire actuel ;
- owner présentation ;
- owner runtime ;
- owner storage/events ;
- Window Manager ;
- parent fonctionnel ;
- diff ;
- handlers/IDs/selectors supprimés ;
- budget timers/observers/fetch/WebSocket/storage ;
- Firefox réel.

Une Build qui corrige sa cible mais casse ailleurs est REFUSÉE.

Ne demande pas à l’opérateur de découvrir à ta place si ton code a détruit une autre section. Fais le maximum de non-régression statique avant livraison, puis Firefox reste l’autorité comportementale.

## CONTRAT CRITIQUE

HOT / immédiatement disponible : Market Core, Binance LIVE, Graphique, Target Top 5, Math / Graph Context V7, auth minimale, CURRENT / IndexedDB continuity, cockpit Atlas critique déjà présent, Oracle summary critique, navigation + Window Manager shells.

COLD / demand possible : Learning/Simulation lourds, Tests/Commandes, Storage/Grey détails, News lourde, Metals, Memory Health / Retrospective, Source Intelligence, gros rapports Atlas, Journal/Scanner/Multi-Collector/Book audits/Decision Board details.

INTERDICTION : ne jamais refaire `.67/.68 → .80` où le cockpit critique Atlas naît tard puis nécessite hydrate→rebind→replay→repair.

`CURRENT` vit indépendamment de l’écran.

## RUNTIME COURANT / SECTION 04

40.4.85 avait cassé la famille 04 en faisant `replaceWith()` sur Storage Health + Grey Plate après capture Window Manager. 40.4.88 a retiré ce propriétaire de remplacement.

Protège : Storage Health resident ; Grey Plate resident ; Window Manager root identity.

System target futur : Simulation = generic closed-body residency pour l’instant ; Commandes = true-lazy seulement ; Backend = true-lazy seulement ; Safety = true-lazy seulement ; Physical Security = true-lazy seulement.

## CANDIDATE A — PREMIER GATE

Il existe un staging non-live :

`coordination/inter_ai_dialogues/agent_crypto/candidate_a_pass10_staging/`

Candidate A veut seulement :

- retirer `projects-demand-residency.js` du parser load graph ;
- retirer `operations-demand-residency.js` du parser load graph ;
- réduire System generic residency à Simulation ;
- passer `residency-audit.js` à un VNext owner-aware ;
- mettre à jour `architecture/administrator-ownership.json` ;
- versionner manifests seulement après PASS.

Six fichiers théoriques : index.html ; js/views/system-demand-residency.js ; js/views/residency-audit.js ; architecture/administrator-ownership.json ; version.json ; administrator-version.json.

Static checks sont déjà PASS.

NE CRÉE PAS DE BUILD tant que Firefox baseline-vs-staging n’est pas réellement validé.

Pass 14 contient le protocole exact de neuf hashes Projects/Operations + Window Manager + protected smoke. Baseline defect existant ne doit pas être « réparé discrètement » dans Candidate A si hors scope.

## ATLAS ROUTING — CANDIDATE SÉPARÉE

Gap prouvé : `#auto-reader`, `#shared-memory`, `#github-memory` sont absents du DOM à froid car leurs bodies sont true-lazy. Command Center/module picker les ciblent pourtant. `administratorHashFamily40361()` dépend de `document.getElementById(decoded)` et ne peut pas classer un target absent.

Pass 16 prouve le handoff correct : ne jamais fetch `views/atlas.html` dans le router ; ne jamais dupliquer `hydrate()` privé ; si déjà hydraté resolve immédiatement ; sinon listener one-shot `erith:presentation-resident` AVANT ouverture ; `details.open=true` déclenche le vrai lazy owner ; attendre `{family:"atlas", key}` ; resolve ID canonique ; scroll/focus si route generation encore courante.

Pass 17 prouve : source fetch partagé ; aucun per-key in-flight hydration lock ; repeated same-key peut théoriquement doubler replace/rebind/event ; close-during-load = cancel ; rejected sourcePromise reste rejetée/cachée ; router doit dédupliquer pending par key + génération anti-stale-focus ; router ne doit PAS inventer retry policy.

Pass 18 prouve : Command Center shell appartient au parser `index.html` ; app.js inspecté n’est pas owner exact du router ; module picker exact handler owner reste NON RÉSOLU parce que la recherche globale était incomplète.

DONC : avant coder le router, énumère exhaustivement tous les scripts réellement chargés et ferme le propriétaire de `#atlasV2AdvancedModuleSelect`. Ne crée jamais un deuxième handler au hasard.

Ne mélange pas router avec `insertAdjacentHTML` refactor.

## ATLAS INSERTADJACENTHTML

`atlas-peripheral-lazy.js` monkeypatch temporairement `Element.prototype.insertAdjacentHTML`, intercepte Atlas host puis restaure la méthode. Dette HIGH-RISK, séparée. Ne la touche pas avec Candidate A ni avec le premier router.

## NO-LOCAL-PRODUCER / RYZEN

Réalité : aucun Ryzen producteur local.

Code historique prouvé : `atlasDeviceComputeSuggestedRole()` → observer seulement si Transformer Book ; sinon production par défaut ; auth + compute allowed peut démarrer `atlasLocalBridgeAutoStart()` ; interval=60 s ; health timeout=8 s.

Future candidate séparée : Admin auth ≠ local producer ; explicit local producer configured ; défaut observer/no-local-producer si absent ; aucun timer Bridge/Ollama si absent ; Ryzen OFFLINE ; CPU/GPU N/A ; Ollama N/A ; Atlas local indisponible ; CURRENT/mémoires historiques conservés en lecture.

Ne jamais afficher 0 % comme si une machine absente était idle.

## LEARNING

40.4.82 a déjà rendu le runtime Learning demand. 40.4.47 est une recovery de présentation PROTÉGÉE après l’échec 40.4.44 : valid DOM → sibling range detach → shell → source `views/system.html` → clone/rebind. Dette : le contenu est encore parsé avant retrait. Future : boundary/source Learning autonome. INTERDIT : regex/string parser gate destructif.

## ORACLE

CLOSED. `oracle-presentation.js` = owner unique. `oracle-demand-residency.js` = tombstone. Ne recrée pas un second owner.

## BACKEND / SOURCE INTELLIGENCE

WATCH seulement. `private-source-demand-loader.js` = demand. `private-backend-sources.js` écoute `erith:system-hydrated` et remonte correctement dans Backend après hydration. Ne réécris pas sans preuve de bug.

## GRAPH DEFECT HISTORIQUE

Ne pas oublier : Graphique peut rester vide/chargement alors que les données continuent, puis `Réinitialiser` rend la main. Ce n’est PAS prouvé corrigé. Si reproduit : audit ciblé chemin normal vs Reset — pending/loading/abort/generation/request id/cache/source state/redraw. Ne mélange pas ça à Candidate A.

## AETHER HEADER

Une seule lane.

A : `[ Relancer ][ Rafraîchir ][ DÉCISION ][ SOURCES ][ DATE / HEURE ]`

B même emplacement/hauteur : `[ ♥ AETHER · Atlas CURRENT · Oracle… · Sources… · Book… · Dernière veille… → ]`

Pas de seconde ligne, pas de header plus haut, pas de Graph shift. Bouton Aether + panneau lazy restent. Météo future = Eure-et-Loir. Machine future = OFFLINE/N/A si aucun producteur.

## PACKAGING

Build runtime : un ZIP unique ; seulement fichiers à uploader ; arborescence GitHub exacte ; aucun README/COMMIT_MESSAGE/audit ; aucun fichier parasite ; aucune chirurgie manuelle. Toujours donner ZIP + cible + commit + test.

## PUBLICATION

Les candidats/handoffs/audits vont d’abord dans :

`coordination/inter_ai_dialogues/agent_crypto/`

Ne touche PAS au runtime live `public/agent_crypto_erith_ia/administrator/` simplement pour publier une candidate. Live runtime uniquement sur demande explicite après gates.

## ORDRE STRICT

1. Vérifier GitHub + Pass nouveaux.
2. Finir/valider Candidate A, sans l’élargir.
3. Atlas router séparé, après fermeture owner module picker.
4. no-local-producer séparé.
5. Learning boundary séparé.
6. Atlas insertAdjacentHTML refactor séparé.
7. diagnostics/architecture truth.
8. monolithe graduel.
9. seulement ensuite revue UI/fonctions Administrator famille par famille, backend, Status Ribbon, Kraken/read-only puis autres projets.

Ne « solde » jamais cinq dettes dans une seule Build. Solde une dette par preuve, puis verrouille.

Tu peux utiliser le web et GitHub, mais le GitHub PUBLIC courant est l’autorité de travail. Les sources privées/archives ne servent que si elles changent réellement une décision ou une transplantation.

Fin du prompt.
