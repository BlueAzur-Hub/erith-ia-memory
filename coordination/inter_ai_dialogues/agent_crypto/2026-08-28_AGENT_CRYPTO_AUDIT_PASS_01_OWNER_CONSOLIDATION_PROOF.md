# Agent-Crypto @erith.IA — Audit cumulatif pass 01

Date: 2026-08-28
Mode: READ-ONLY RUNTIME AUDIT
Runtime authority checked: `public/agent_crypto_erith_ia/administrator/`
Runtime build: `40.4.88`
Market Core: `38.15.11` — PROTECTED
This file is a coordination report only. It does not modify the live Administrator.

## 1. HEAD / runtime truth

GitHub HEAD has advanced after the 40.4.88 runtime commit because the end-of-thread checkpoint, reprise prompt and protocol handoff were published.

The live Administrator runtime itself remains `40.4.88` and `version.json` still declares:

- build `40.4.88`;
- parent `40.4.87`;
- release `SYSTEM 04 INTERACTION RECOVERY · VERSION TRUTH LOCK`;
- Market Core reference `38.15.11`.

Therefore: do not confuse repository HEAD with runtime build identity.

## 2. Oracle owner consolidation — CLOSED confirmed

Current evidence confirms the handoff:

- `oracle-presentation.js` 40.4.41 is the canonical true-lazy presentation owner;
- it owns the three heavy subsection shells, hydration and exclusive accordion behavior;
- `oracle-demand-residency.js` is an inert tombstone;
- no new Oracle duplicate owner was found in the current production load graph.

Status remains `CLOSED / PROTECTED`.

## 3. Projects generic residency — PROVEN REDUNDANT AT PRESENTATION LAYER

Current facts:

- `projects-presentation.js` 40.4.20 mounts the Missions hero + five stable `<details>` shells;
- each heavy body is absent at boot and hydrates only on first disclosure;
- each summary contains a stable routing anchor;
- Window Manager membership is resolved independently by `missionEntries40302()` from the real top-level shell children and collapse keys;
- shared runtime does not use `ErithPresentationLifecycle` as Window Manager authority.

The legacy `projects-demand-residency.js` 40.4.12 therefore no longer owns heavy-body boot relief. It mainly detaches already-empty placeholder bodies after `window.load`.

Analytical status: `PROVEN RETIRABLE`, but runtime file retirement still requires a candidate build + validation.

Required validation before live retirement:

- Missions quick-link/hash open;
- Window Manager Reduce/Hide/Detach;
- open → close → reopen for all five projects;
- no duplicate IDs after hydration;
- Firefox interaction.

## 4. Operations generic residency — PROVEN REDUNDANT AT PRESENTATION LAYER

Current facts:

- `operations-presentation.js` 40.4.21 mounts stable shells for Situation / Questionnaire / Briefing / Planning;
- bodies are absent at boot;
- hydration owns its own toggle path;
- runtime actions are explicitly rebound after hydration;
- Window Manager family 03 membership is independently resolved from the real top-level details using `ADMIN_OPERATION_KEYS_40308`;
- generic lifecycle registration is not the Window Manager authority.

Therefore `operations-demand-residency.js` 40.4.13 is also analytically `PROVEN RETIRABLE`.

Required validation before live retirement:

- direct section navigation;
- questionnaire save/build/copy/download/clear action rebind;
- command preset rebind;
- Window Manager compact/minimize/restore;
- Firefox reopen behavior.

## 5. System 04 — mixed owner proof strengthened

Window Manager explicitly resolves family 04 by stable real nodes:

- family header;
- Storage Health;
- Grey Plate Forensic;
- Simulation;
- Commandes;
- Backend;
- Safety;
- Physical Security.

This confirms why the 40.4.85 `replaceWith()` of Storage/Grey violated ownership.

Current architecture:

- Storage = stable resident root;
- Grey Plate = stable resident root;
- Simulation = real parser-mounted heavy body;
- Commandes = true-lazy shell/body;
- Backend = true-lazy shell/body;
- Safety = true-lazy shell/body;
- Physical Security = true-lazy shell/body.

`system-presentation.js` already owns hydration for the last four. `private-backend-sources.js` explicitly reconciles after `erith:system-hydrated`.

Analytical target remains:

- generic closed-body residency retained for Simulation;
- generic closed-body residency retired for Commandes / Backend / Safety / Physical Security;
- Storage + Grey remain resident.

Status: `PROVEN TARGET`, not yet runtime-applied.

## 6. Atlas — IMPORTANT navigation debt discovered

The previous handoff correctly identified duplicate ownership:

- `atlas-peripheral-lazy.js` true-lazies Auto Reader, Shared Memory, GitHub Memory;
- `atlas-family-demand-residency.js` still registers those three plus `#atlas-local-ai-collapse`.

However a new navigation fact changes the retirement order.

`index.html` contains real Administrator quick-links:

- `href="#auto-reader"`;
- `href="#shared-memory"`.

But in `atlas-presentation.js`, the IDs `auto-reader` and `shared-memory` are inside the heavy bodies, not in stable summary-shell anchors.

`atlas-peripheral-lazy.js` removes/replaces these bodies before the live Atlas presentation is inserted.

Therefore at cold boot the hash targets themselves may not exist until hydration. The old generic residency owner cannot solve that missing-anchor problem because the body has already been preprocessed into a lazy placeholder before generic closed-body residency later runs.

This is a current latent navigation debt, separate from generic residency.

New rule:

**Do not retire Atlas peripheral generic residency before designing stable shell anchors / explicit hash-to-key routing for Auto Reader, Shared Memory and GitHub Memory.**

The correct future order is:

1. add/verify stable shell anchors or explicit key routing;
2. prove quick-links open the correct `<details>` from cold boot;
3. prove hydration occurs exactly once;
4. then retire redundant generic residency for the three peripherals;
5. keep `#atlas-local-ai-collapse` generic residency unless separately proven unnecessary.

Status D-07: `OPEN-SAFE`, but retirement is BLOCKED by navigation proof.

## 7. Diagnostics

`residency-audit.js` remains stale because it still treats Projects/Operations/System/Atlas as required generic registrations and Oracle as the only true-lazy family.

`architecture-freeze.js` remains historical/stale and must not gate a modern owner-consolidation build until modernized or tombstoned.

No cold-boot penalty from these diagnostics because they are demand-loaded.

## 8. Protected invariants

No change proposed to:

- Market Core 38.15.11;
- Binance LIVE;
- Graph Context V7;
- Graphique;
- Target Top 5;
- CURRENT critical state/finalization;
- Oracle canonical owner;
- IndexedDB schema;
- Aether single-lane header geometry;
- Window Manager node identity.

## 9. Next audit target

Priority order for next pass:

1. prove exact current hash/router behavior for Atlas lazy peripherals and design the smallest stable-anchor contract;
2. inspect no-local-producer / Ryzen role functions in current shared runtime, including Bridge supervision timers and default role semantics;
3. update the proposed modern residency truth model before any owner retirement candidate;
4. only then consider a small candidate build combining owner consolidation + diagnostic truth, with no monolith refactor.

No runtime Build is authorized by this report alone.
