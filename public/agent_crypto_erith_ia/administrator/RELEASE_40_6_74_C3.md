# Agent-Crypto Administrator — 40.6.74 Candidate 3

## Aether Watch V2 — Content Density + Workbench Refinement

Parent field state: **40.6.74 C2.1** on the validated **40.6.73 Administrator** runtime / **Market Core 38.15.11**.

C2.1 validates the V2 backplate activation path and gives Aether substantially more spatial headroom. Field review still shows excessive density in Atlas and Recent Events, clipped machine-state strings, and a Workbench presentation that remains visually tied to its historical 40.6.39 subsystem label.

### C3 correction

A new presentation-only sheet is loaded last:

`aether-v2-refinement-406074-c3.css`

It refines the existing V2 without changing Aether data ownership.

#### Aether Watch

- Atlas: score remains primary; long machine-state strings are ellipsized and secondary diagnostic text yields in medium/floating layouts.
- Recent Events: maximum three rows at large size, two useful rows in medium/floating Aether; event prose is single-line ellipsis instead of a clipped paragraph wall.
- Divergence: three-line allowance at large size, two-line contract in medium layouts.
- Convergence: the `3/4` verdict remains dominant; supporting footer yields before the primary signal.
- Market: breadth and asset values remain aligned; labels may truncate without destroying values.
- Sources / Oracle / Weather: spacing is disciplined without further type inflation.
- Center: attention level remains dominant; tertiary footer disappears before the core state is reduced.

#### Aether Workbench

The existing read-only Workbench runtime remains the interaction/data owner. C3 only harmonizes presentation:

- candidate truth displayed as `AETHER WORKBENCH · 40.6.74 C3`;
- History rows are lighter and more compact;
- Details cards use reduced padding and calmer typography;
- no change to Events / History / Details data semantics, drag, maximize, close or z-order behavior.

### Protected

- global visible Administrator build remains **40.6.73** until Firefox field PASS;
- Market Core **38.15.11** unchanged;
- Administrator Window Manager unchanged;
- V2 backplate binary unchanged;
- R5 active Crypto fiche foreground behavior preserved;
- Aether runtime/data owners unchanged;
- Atlas / Oracle / Graph / Lecture Technique data logic unchanged;
- no timer;
- no MutationObserver;
- no storage owner;
- no network owner.

### Field acceptance

1. Open Aether floating over the Crypto/Oracle workspace.
2. Confirm Atlas no longer shows several competing clipped `PENDING...` strings.
3. Confirm Recent Events presents two clean rows at medium size and up to three at large size.
4. Maximize Aether and verify Market / Sources / Oracle / Weather retain breathing room.
5. Open `Historique` and `Détails`; confirm the Workbench is visually lighter and identifies the candidate as 40.6.74 C3.
6. Open a Crypto fiche over Aether and confirm R5 still keeps the fiche in front.

If C3 receives field PASS, the next operation is **not another patch**: consolidate the validated V2 spatial, responsive and refinement rules into a canonical clean 40.6.74 Aether implementation, remove redundant candidate layers, reread the full Aether zone, then promote build truth.
