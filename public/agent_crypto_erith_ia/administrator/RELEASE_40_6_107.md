# Agent-Crypto 40.6.107 — Visible Version Surface Truth

Parent: **40.6.106**  
Market Core: **38.15.11** — unchanged.

## Purpose

Close the reproduced current-runtime version leak without rewriting historical provenance or protected business owners.

Reproduced on Firefox Administrator 40.6.106:

- `ATLAS AUTO READER V4 — Build 40.6.86 · Administrator`;
- `audit statique FAIL [interface_build_truth]` despite an immutable 40.6.106 entry;
- a current footer capable of rendering the parser-time legacy build.

## Owner

`js/visible-version-surface-truth-406107.js`

The patch reads the current loaded build from `ErithVersionTruth`, with immutable pathname fallback. It only normalizes the smallest current Auto Reader output containing the exact Auto Reader/audit tokens and `#footerRelease`.

`interface_build_truth` is promoted to PASS only when pathname/query authority, Version Truth owner, current metadata and Version Truth dataset agree. A disagreement remains FAIL.

## Preserved

- Market Core 38.15.11;
- Web Classic;
- Atlas CURRENT business logic;
- Oracle;
- Lecture Technique;
- Strategy A / TRADUS;
- market data and prices;
- historical/archive provenance;
- storage schemas.

No recurring timer, MutationObserver, network owner, storage writer, wallet or trading endpoint is added.

## Acceptance

Static pure self-test: **4/4 expected**.

Terrain proof after deployment:

1. open immutable Administrator 40.6.107;
2. open Atlas Auto Reader;
3. require `Build 40.6.107 · Administrator`;
4. require `audit statique PASS [interface_build_truth]` only if deterministic authorities agree;
5. export the full report and verify the current footer no longer advertises Build 40.6.86.
