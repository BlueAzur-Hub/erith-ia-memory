/* Agent-Crypto Administrator — RETIRED BOOT COMPATIBILITY FILE.
   Active boot ownership moved to the release entry (index.html) in 40.6.124.
   This file intentionally performs no boot, no manifest read, no version write,
   no navigation and no reload. It remains only so old cached references fail
   closed instead of resurrecting the former build.json-as-loaded-build model. */
(() => {
  "use strict";
  try {
    document.documentElement.dataset.legacyBoot406124 = "retired";
  } catch (_) {}
})();
