/* Agent-Crypto @erith.IA — 40.4.67
   ATLAS FAMILY TRUE SOURCE-LAZY HYDRATION / LATE RUNTIME REBIND LOCK
   Family header + five stable disclosure shells mount during parsing.
   Canonical Atlas markup stays in views/atlas.html and is fetched once only after
   an Atlas disclosure is opened. The heavy local-AI body is not built at boot.
   CURRENT/Bridge/Memory/Auto Reader/GitHub Memory engines stay runtime-owned by app.js.
   CURRENT audit + Book/Knowledge bodies remain second-level on-demand readers.
   No recurring timer, observer, storage owner, model reset or business-data owner added. */
(()=>{
  "use strict";
  const BUILD="40.4.67";
  const SOURCE="./views/atlas.html";
  const host=document.getElementById("atlas-view-host");
  if(!host)return;

  const shellHtml=`
    <section class="atlas-layout-family atlas-layout-family-intelligence" aria-labelledby="atlasLayoutFamily02" data-atlas-source-lazy-40467="header">
      <span class="atlas-layout-family-index" aria-hidden="true">02</span>
      <div class="atlas-layout-family-copy">
        <p>PARCOURS ADMINISTRATEUR</p>
        <h2 id="atlasLayoutFamily02" class="atlas-icon-heading-40290" data-semantic-tone-40290="intelligence"><span class="atlas-heading-icon-40290" aria-hidden="true"><svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="6" cy="12" r="2"></circle><circle cx="12" cy="6" r="2"></circle><circle cx="18" cy="12" r="2"></circle><circle cx="12" cy="18" r="2"></circle><path d="M7.5 10.5 10.5 7.5M13.5 7.5l3 3M16.5 13.5l-3 3M10.5 16.5l-3-3"></path></svg></span>Intelligence, mémoire & création</h2>
        <span>Interroger Atlas et Aerith, conserver les repères, puis ouvrir la Forge sans quitter le parcours.</span>
      </div>
      <span class="atlas-layout-family-signal" aria-hidden="true"></span>
    </section>

    <details class="atlas-collapse glass atlas-local-ai-collapse atlas-family-member atlas-tone-intelligence" id="atlas-local-ai-collapse" data-collapse-key="local-ai-hub" data-layout-family="intelligence" data-atlas-source-lazy-shell-40467="local-ai-hub">
      <summary class="atlas-collapse-summary">
        <span class="atlas-collapse-icon" aria-hidden="true">▶</span>
        <span class="atlas-collapse-title">Atlas-10 + Aerith-10 Crypto</span>
        <span class="atlas-collapse-subtitle">Profils locaux du Ryzen · contenu détaillé chargé à l’ouverture</span>
        <div class="atlas-collapsed-current-preview-40295" id="atlasCollapsedCurrentPreview40295" data-phase="idle" aria-label="Résumé du pipeline CURRENT Atlas">
          <div class="atlas-collapsed-current-copy-40295"><span>PIPELINE LOCAL · CURRENT</span><b id="atlasCollapsedCurrentTitle40295">État runtime conservé</b></div>
          <div class="atlas-collapsed-current-readout-40295"><strong id="atlasCollapsedCurrentPercent40295">0 %</strong><span id="atlasCollapsedCurrentState40295">IDLE</span></div>
          <div class="atlas-collapsed-current-track-40295" aria-hidden="true"><span id="atlasCollapsedCurrentBar40295"></span></div>
          <div class="atlas-analysis-progress-nodes atlas-collapsed-current-nodes-40295" aria-label="Étapes compactes Atlas CURRENT"><span data-atlas-progress-node="1"><i>01</i><b>Marché</b></span><span data-atlas-progress-node="2"><i>02</i><b>Top 5</b></span><span data-atlas-progress-node="3"><i>03</i><b>Math Core</b></span><span data-atlas-progress-node="4"><i>04</i><b>Contradictions</b></span><span data-atlas-progress-node="nox"><i>NØX</i><b>No-FOMO</b></span><span data-atlas-progress-node="aerith"><i>A</i><b>Aerith</b></span></div>
        </div>
      </summary>
      <div class="atlas-collapse-body" data-atlas-source-lazy-body-40467="local-ai-hub" data-atlas-hydration-40467="placeholder"><p class="atlas-local-response-empty">Atlas/Aerith · présentation détaillée chargée uniquement à l’ouverture. Le moteur CURRENT reste résident.</p></div>
    </details>

    <details class="atlas-collapse glass forge-aerith-collapse forge-aerith-embedded-collapse atlas-family-member atlas-tone-creation" id="forge-aerith" data-collapse-key="forge-aerith" data-layout-family="creation" data-atlas-source-lazy-shell-40467="forge-aerith">
      <summary class="atlas-collapse-summary"><span class="atlas-collapse-icon" aria-hidden="true">▶</span><span class="atlas-collapse-title">✦ Forge d’Aerith Pro</span><span class="atlas-collapse-subtitle">Atelier Créatrice · chargé à l’ouverture</span></summary>
      <div class="atlas-collapse-body" data-atlas-source-lazy-body-40467="forge-aerith" data-atlas-hydration-40467="placeholder"></div>
    </details>

    <details class="atlas-collapse glass atlas-family-member atlas-tone-intelligence" data-collapse-key="auto-reader" data-layout-family="intelligence" data-atlas-source-lazy-shell-40467="auto-reader">
      <summary class="atlas-collapse-summary"><span class="atlas-collapse-icon" aria-hidden="true">▶</span><span class="atlas-collapse-title">Atlas Auto Reader</span><span class="atlas-collapse-subtitle">Collecte autonome du marché · présentation à la demande</span></summary>
      <div class="atlas-collapse-body" data-atlas-source-lazy-body-40467="auto-reader" data-atlas-hydration-40467="placeholder"></div>
    </details>

    <details class="atlas-collapse glass atlas-family-member atlas-tone-intelligence" data-collapse-key="shared-memory" data-layout-family="intelligence" data-atlas-source-lazy-shell-40467="shared-memory">
      <summary class="atlas-collapse-summary"><span class="atlas-collapse-icon" aria-hidden="true">▶</span><span class="atlas-collapse-title">Shared Memory</span><span class="atlas-collapse-subtitle">Export / import par machine · présentation à la demande</span></summary>
      <div class="atlas-collapse-body" data-atlas-source-lazy-body-40467="shared-memory" data-atlas-hydration-40467="placeholder"></div>
    </details>

    <details class="atlas-collapse glass atlas-family-member atlas-tone-intelligence" data-collapse-key="github-memory" data-layout-family="intelligence" data-atlas-source-lazy-shell-40467="github-memory">
      <summary class="atlas-collapse-summary"><span class="atlas-collapse-icon" aria-hidden="true">▶</span><span class="atlas-collapse-title">GitHub Shared Memory</span><span class="atlas-collapse-subtitle">Mémoire commune GitHub · présentation à la demande</span></summary>
      <div class="atlas-collapse-body" data-atlas-source-lazy-body-40467="github-memory" data-atlas-hydration-40467="placeholder"></div>
    </details>`;

  const SPECS=Object.freeze({
    "local-ai-hub":Object.freeze({id:"atlas-local-ai-collapse",selector:'#atlas-local-ai-collapse',rebind:"local-ai"}),
    "forge-aerith":Object.freeze({id:"forge-aerith",selector:'#forge-aerith',rebind:"forge"}),
    "auto-reader":Object.freeze({selector:'details[data-collapse-key="auto-reader"]',rebind:"auto-reader"}),
    "shared-memory":Object.freeze({selector:'details[data-collapse-key="shared-memory"]',rebind:"shared-memory"}),
    "github-memory":Object.freeze({selector:'details[data-collapse-key="github-memory"]',rebind:"github-memory"})
  });
  const AUDIT=Object.freeze({
    "stable-stack":Object.freeze({id:"atlasStableStack",titleId:"atlasStableStackTitle",title:"Interface, Control Center, Bridge et mémoire",eyebrow:"PILE STABLE CONSOLIDÉE · PRODUCTION / LECTURE",scope:"current-audit"}),
    "analytical-truth":Object.freeze({id:"atlasAnalyticalTruth",titleId:"atlasAnalyticalTruthTitle",title:"Contexte, sources, preuves et qualité statistique",eyebrow:"VÉRITÉ ANALYTIQUE · EMPREINTE V2",scope:"current-audit"}),
    "frame-truth":Object.freeze({id:"atlasFrameTruth",titleId:"atlasFrameTruthTitle",title:"Snapshot analysé et marché live",eyebrow:"COHÉRENCE TEMPORELLE · SNAPSHOT / LIVE",scope:"current-audit"}),
    "current-truth":Object.freeze({id:"atlasCurrentTruth33",titleId:"atlasCurrentTruth33Title",title:"Pourquoi cette analyse est CURRENT",eyebrow:"CURRENT TRUTH · ÉTAT TRANSACTIONNEL",scope:"current-audit"}),
    "book-readonly":Object.freeze({id:"atlasBookReadOnlyKnowledge",titleId:"atlasBookReadOnlyTitle",title:"Miroir et lecture Book",eyebrow:"SHARED READ-ONLY KNOWLEDGE · RYZEN → BOOK",scope:"book-knowledge"}),
    "knowledge-library":Object.freeze({id:"atlasKnowledgeLibrary",titleId:"atlasKnowledgeLibraryTitle",title:"Dictionnaire Crypto / Banque / Bourse",eyebrow:"BIBLIOTHÈQUE PÉDAGOGIQUE PERMANENTE · SANS BRIDGE",scope:"book-knowledge"})
  });

  let sourcePromise=null,sourceTextCache="",sourceFetchCount=0,hydrationCount=0,subsectionHydrationCount=0,lastError="";
  const hydrated=new Set(),subsectionHydrated=new Set();

  host.insertAdjacentHTML("beforebegin",shellHtml);
  host.remove();

  const paintStyle=document.createElement("style");
  paintStyle.id="atlasSourceLazyPaintIsolation40467";
  paintStyle.textContent='[data-atlas-source-lazy-shell-40467] > .atlas-collapse-body[data-atlas-hydration-40467="ready"]{content-visibility:auto;contain-intrinsic-size:auto 720px;}';
  document.head.appendChild(paintStyle);

  const escRe=value=>String(value).replace(/[.*+?^${}()|[\]\\]/g,"\\$&");
  function elementBounds(source,tag,matcher){
    const startRe=matcher instanceof RegExp?matcher:new RegExp(`<${tag}\\b[^>]*${matcher}[^>]*>`,`i`);
    const hit=startRe.exec(source); if(!hit)return null;
    const start=hit.index,openEnd=start+hit[0].length,token=new RegExp(`<\\/?${tag}\\b[^>]*>`,`ig`);
    token.lastIndex=start; let depth=0,m;
    while((m=token.exec(source))){
      if(new RegExp(`^<\\/${tag}`,"i").test(m[0])) depth-=1; else depth+=1;
      if(depth===0)return {start,openEnd,close:m.index,end:token.lastIndex};
    }
    return null;
  }
  function detailBounds(source,keyOrId){
    const key=escRe(keyOrId);
    const re=keyOrId.startsWith("#")
      ? new RegExp(`<details\\b[^>]*\\bid=["']${escRe(keyOrId.slice(1))}["'][^>]*>`,`i`)
      : new RegExp(`<details\\b[^>]*data-collapse-key=["']${key}["'][^>]*>`,`i`);
    return elementBounds(source,"details",re);
  }
  function sectionBounds(source,id){
    return elementBounds(source,"section",new RegExp(`<section\\b[^>]*\\bid=["']${escRe(id)}["'][^>]*>`,`i`));
  }
  function sourceText(){
    if(sourceTextCache)return Promise.resolve(sourceTextCache);
    if(sourcePromise)return sourcePromise;
    sourceFetchCount+=1;
    sourcePromise=fetch(SOURCE,{credentials:"same-origin",cache:"default"})
      .then(response=>{if(!response.ok)throw new Error(`Atlas source HTTP ${response.status}`);return response.text();})
      .then(text=>{sourceTextCache=String(text||"");lastError="";return sourceTextCache;})
      .catch(error=>{lastError=String(error?.message||error||"Atlas source unavailable");sourcePromise=null;throw error;});
    return sourcePromise;
  }
  function auditShellHtml(key,spec){
    return `<div class="section-head compact atlas-current-audit-shell-40467" data-atlas-current-audit-shell-40467="${key}"><div><p class="eyebrow">${spec.eyebrow}</p><h5 id="${spec.titleId}">${spec.title}</h5><p class="planning-intro">État runtime conservé · détails matérialisés uniquement à la demande.</p></div><button class="btn" type="button" data-atlas-source-lazy-section-40467="${key}" aria-controls="${spec.id}">Ouvrir les détails</button></div>`;
  }
  function stripAuditSections(html){
    let next=String(html||"");
    for(const [key,spec] of Object.entries(AUDIT)){
      const b=sectionBounds(next,spec.id); if(!b)continue;
      const open=next.slice(b.start,b.openEnd);
      next=next.slice(0,b.start)+open+auditShellHtml(key,spec)+`</section>`+next.slice(b.end);
    }
    return next;
  }
  function sourceDetailHtml(source,key){
    const spec=SPECS[key]; if(!spec)return "";
    const b=spec.id?detailBounds(source,`#${spec.id}`):detailBounds(source,key);
    if(!b)return "";
    let html=source.slice(b.start,b.end);
    if(key==="local-ai-hub")html=stripAuditSections(html);
    return html;
  }
  function sourceSectionInner(source,id){
    const b=sectionBounds(source,id); return b?source.slice(b.openEnd,b.close):"";
  }
  function liveDetail(key){return document.querySelector(`[data-atlas-source-lazy-shell-40467="${CSS.escape(key)}"]`);}
  function liveBody(key){return liveDetail(key)?.querySelector(':scope > .atlas-collapse-body')||null;}
  function parseDetailBody(html,key){
    const template=document.createElement("template"); template.innerHTML=html;
    const sourceDetail=template.content.querySelector(SPECS[key].selector);
    const sourceBody=sourceDetail?.querySelector(':scope > .atlas-collapse-body');
    if(!(sourceBody instanceof HTMLElement))throw new Error(`Atlas body missing: ${key}`);
    return [...sourceBody.childNodes].map(node=>node.cloneNode(true));
  }
  function bindAuditButtons(root=document){
    root.querySelectorAll?.('[data-atlas-source-lazy-section-40467]')?.forEach(button=>{
      if(button.dataset.atlasSourceLazyBound40467==="1")return;
      button.dataset.atlasSourceLazyBound40467="1";
      button.addEventListener("click",()=>void hydrateSubsection(button.dataset.atlasSourceLazySection40467));
    });
  }
  function rebind(scope){
    try{globalThis.AgentCryptoAtlasLateHydration40467?.rebind?.(scope);}catch(error){console.warn(`Agent-Crypto ${BUILD} · Atlas late rebind`,scope,error);}
    try{if(scope!=="local-ai"&&scope!=="forge")globalThis.AgentCryptoAtlasPeripheralRebind?.rebind?.(scope);}catch(_){}
    try{globalThis.atlasV2ClassifySections?.();}catch(_){}
    try{const mode=globalThis.atlasV2Mode?.()||document.documentElement.dataset.atlasView||"essential";globalThis.atlasV2ApplySectionVisibility?.(mode);globalThis.atlasV2ApplySemanticRoleIsolation40312?.(mode);}catch(_){}
    try{window.dispatchEvent(new CustomEvent("erith:atlas-hydrated",{detail:{build:BUILD,scope}}));}catch(_){}
  }
  async function hydrateSubsection(key){
    const spec=AUDIT[String(key||"")]; if(!spec)return false;
    const targets=spec.scope==="book-knowledge"
      ? Object.entries(AUDIT).filter(([,row])=>row.scope==="book-knowledge"&&!subsectionHydrated.has(row.id))
      : [[String(key),spec]];
    if(!targets.length){rebind(spec.scope);return true;}
    try{
      const source=await sourceText();
      for(const [,row] of targets){
        if(subsectionHydrated.has(row.id))continue;
        const live=document.getElementById(row.id); if(!live)continue;
        live.dataset.atlasHydration40467="loading";
        const inner=sourceSectionInner(source,row.id); if(!inner)throw new Error(`Atlas subsection missing: ${row.id}`);
        const template=document.createElement("template"); template.innerHTML=inner;
        live.replaceChildren(template.content.cloneNode(true));
        live.dataset.atlasHydration40467="ready";
        subsectionHydrated.add(row.id); subsectionHydrationCount+=1;
      }
      rebind(spec.scope); return true;
    }catch(error){lastError=String(error?.message||error||"unknown");return false;}
  }
  async function hydrate(key){
    key=String(key||""); const spec=SPECS[key]; if(!spec)return false;
    const detail=liveDetail(key); let body=liveBody(key);
    if(!(detail instanceof HTMLDetailsElement))return false;
    if(!(body instanceof HTMLElement)){
      try{globalThis.ErithPresentationLifecycle?.restoreForHash?.(`#${detail.id||detail.dataset.collapseKey||""}`);}catch(_){}
      body=liveBody(key);
    }
    if(!(body instanceof HTMLElement))return false;
    if(body.dataset.atlasHydration40467==="ready"){rebind(spec.rebind);return true;}
    if(body.dataset.atlasHydration40467==="loading"){try{await sourcePromise;}catch(_){}return body.dataset.atlasHydration40467==="ready";}
    body.dataset.atlasHydration40467="loading";
    try{
      const source=await sourceText();
      const html=sourceDetailHtml(source,key); if(!html)throw new Error(`Atlas detail missing: ${key}`);
      body.replaceChildren(...parseDetailBody(html,key));
      body.dataset.atlasHydration40467="ready";
      hydrated.add(key); hydrationCount+=1;
      if(key==="local-ai-hub")bindAuditButtons(body);
      rebind(spec.rebind); return true;
    }catch(error){
      body.dataset.atlasHydration40467="error";
      body.innerHTML=`<p class="atlas-local-response-empty">Présentation Atlas indisponible · ${String(error?.message||error||"erreur inconnue")}</p>`;
      lastError=String(error?.message||error||"unknown");
      console.warn(`Agent-Crypto ${BUILD} · Atlas source hydration failed`,key,error); return false;
    }
  }
  Object.keys(SPECS).forEach(key=>{
    const detail=liveDetail(key); if(!(detail instanceof HTMLDetailsElement))return;
    const ensure=()=>{if(detail.open)queueMicrotask(()=>void hydrate(key));};
    detail.addEventListener("toggle",ensure);
    detail.addEventListener("erith:presentation-resident",ensure);
    if(detail.open)ensure();
  });
  async function ensureTarget(id){
    id=String(id||"").replace(/^#/,"");
    const direct=document.getElementById(id); if(direct)return direct;
    let key="local-ai-hub";
    if(id==="forge-aerith")key="forge-aerith";
    else if(id==="auto-reader")key="auto-reader";
    else if(id==="shared-memory")key="shared-memory";
    else if(id==="github-memory")key="github-memory";
    const detail=liveDetail(key); if(detail)detail.open=true;
    await hydrate(key); return document.getElementById(id)||detail||null;
  }
  function hashHydrate(){
    const id=decodeURIComponent(String(location.hash||"").replace(/^#/,"")); if(!id||document.getElementById(id))return;
    if(/^atlas|^local-ai-hub$|^decision-board$|^auto-reader$|^shared-memory$|^github-memory$|^forge-aerith$/i.test(id)){
      void ensureTarget(id).then(node=>{if(node)requestAnimationFrame(()=>node.scrollIntoView?.({block:"start"}));});
    }
  }
  window.addEventListener("hashchange",hashHydrate,{passive:true});
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",hashHydrate,{once:true});else hashHydrate();

  function snapshot(){return Object.freeze({
    build:BUILD,source:SOURCE,strategy:"parser-shell + one-source on-demand Atlas hydration",
    boot_full_atlas_html:false,legacy_peripheral_lazy_owner_loaded:false,source_fetch_count:sourceFetchCount,
    source_text_cached:!!sourceTextCache,hydration_count:hydrationCount,subsection_hydration_count:subsectionHydrationCount,
    hydrated:[...hydrated],subsections:[...subsectionHydrated],last_error:lastError||null,
    current_runtime_owner:"app.js + IndexedDB",book_mirror_runtime_preserved:true,auto_reader_runtime_preserved:true,
    github_memory_runtime_preserved:true,new_recurring_timer:false,new_observer:false,storage_owner_added:false,
    business_engine_changed:false,market_core_changed:false,current_changed:false,oracle_changed:false
  });}
  const api=Object.freeze({build:BUILD,source:SOURCE,ensureBody:hydrate,ensureTarget,hydrateSubsection,snapshot,keys:Object.keys(SPECS),new_recurring_timer:false,new_observer:false,storage_owner_added:false});
  globalThis.ErithAtlasPresentation=api;
  globalThis.ErithAtlasPresentation40467=api;
  globalThis.AgentCryptoAtlasPeripheralLazy=Object.freeze({build:BUILD,retired_legacy_owner:true,replaced_by:"ErithAtlasPresentation40467",runtime_owner:"app.js"});
  globalThis.__AGENT_CRYPTO_ATLAS_PRESENTATION_40467__=snapshot();
})();
