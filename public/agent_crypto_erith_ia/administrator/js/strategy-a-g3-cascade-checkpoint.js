/* Agent-Crypto @erith.IA — 40.6.199 G3 REPLAY DATASET CERTIFICATION
   Cumulative checkpoint: exact real 24 h market window + strict historical t0
   traceability + immutable join candidate. Read-only, PAPER ONLY, no outcomes,
   no profitability claim, no Gate promotion, no real execution path. */
(() => {
  "use strict";
  const BUILD="40.6.199",ROOT_ID="strategyAG3CascadeCheckpoint",DOSSIER_ID="strategyADossier",DAY_MS=86400000;
  const finite=v=>{const n=Number(v);return Number.isFinite(n)?n:null;},parseTs=v=>{const n=typeof v==="number"?v:Date.parse(String(v??""));return Number.isFinite(n)?n:null;};
  const first=(o,paths)=>{for(const p of paths){let v=o;for(const k of p.split(".")){v=v?.[k];if(v===undefined||v===null)break;}if(v!==undefined&&v!==null&&v!=="")return v;}return null;};
  const median=a=>{const b=a.filter(Number.isFinite).sort((x,y)=>x-y);if(!b.length)return null;const m=Math.floor(b.length/2);return b.length%2?b[m]:(b[m-1]+b[m])/2;};
  function temporal(){
    let raw=null;try{raw=globalThis.AgentCryptoMarketSeriesTruth?.snapshot?.()||null}catch(_){}
    const blockers=[];if(!raw||raw.available===false)return {certified:false,status:"NOT_CERTIFIED",blockers:["SOURCE_UNAVAILABLE"],rows:[],window_points:0};
    if(raw.source_period_proven!==true||Number(raw.source_period_days)!==1)blockers.push("SOURCE_24H_NOT_PROVEN");
    const norm=(Array.isArray(raw.rows)?raw.rows:[]).map(x=>Array.isArray(x)?{t:parseTs(x[0]),p:finite(x[1])}:{t:parseTs(x?.t??x?.ts??x?.time??x?.timestamp??x?.at),p:finite(x?.p??x?.price??x?.value??x?.close)}).filter(r=>r.t!==null&&r.p!==null&&r.p>0).sort((a,b)=>a.t-b.t);
    const uniq=[];for(const r of norm)if(!uniq.length||uniq.at(-1).t!==r.t)uniq.push(r);if(uniq.length<2)return {certified:false,status:"NOT_CERTIFIED",blockers:[...blockers,"INSUFFICIENT_VALID_ROWS"],rows:[],window_points:0};
    const diffs=[];for(let i=1;i<uniq.length;i++)diffs.push((uniq[i].t-uniq[i-1].t)/60000);const cadence=median(diffs),end=uniq.at(-1).t,target=end-DAY_MS,tol=Math.max(1000,(cadence||1)*4800);let idx=-1,best=Infinity;uniq.forEach((r,i)=>{const d=Math.abs(r.t-target);if(d<best){best=d;idx=i;}});const rows=idx>=0?uniq.slice(idx):[],span=rows.length>1?(rows.at(-1).t-rows[0].t)/60000:null,wd=[];for(let i=1;i<rows.length;i++)wd.push((rows[i].t-rows[i-1].t)/60000);const maxGap=wd.length?Math.max(...wd):null,expected=cadence>0?Math.round(1440/cadence)+1:null;
    if(!(cadence>0))blockers.push("OBSERVED_CADENCE_UNKNOWN");if(best>tol)blockers.push("NO_EXACT_24H_BOUNDARY");if(span===null||Math.abs(span-1440)>Math.max(.25,(cadence||1)*.08))blockers.push("SPAN_NOT_24H");if(expected!==null&&rows.length!==expected)blockers.push("ROW_COUNT_MISMATCH");if(cadence>0&&wd.some(d=>Math.abs(d-cadence)>Math.max(.25,cadence*.25)))blockers.push("IRREGULAR_CADENCE");if(cadence>0&&maxGap>cadence*1.25)blockers.push("UNEXPECTED_GAP");
    return Object.freeze({certified:blockers.length===0,status:blockers.length?"NOT_CERTIFIED":"CERTIFIED",asset:String(raw.symbol||raw.asset_id||"BTC").toUpperCase(),rows:Object.freeze(rows.map(r=>Object.freeze([r.t,r.p]))),source_points:norm.length,window_points:rows.length,observed_cadence_min:cadence,window_span_min:span,max_gap_min:maxGap,first_ms:rows[0]?.t??null,last_ms:rows.at(-1)?.t??null,first_at:rows.length?new Date(rows[0].t).toISOString():null,last_at:rows.length?new Date(rows.at(-1).t).toISOString():null,blockers:Object.freeze(blockers)});
  }
  function ledgerRows(){try{const v=globalThis.AgentCryptoStrategyAExperimentLedger?.read?.();return Array.isArray(v)?v:Array.isArray(v?.rows)?v.rows:Array.isArray(v?.entries)?v.entries:[]}catch(_){return[]}}
  function normalizeT0(r){
    const id=first(r,["cycle_id","decision_id","proposal_id","id","trace.cycle_id","trace.id"]),asset=String(first(r,["asset","symbol","market.symbol","inputs.asset","inputs.symbol","snapshot.symbol"])||"").toUpperCase(),decision=String(first(r,["decision","verdict","action","proposal.decision","result.decision","trace.decision"])||"").toUpperCase();
    const marketAt=parseTs(first(r,["market_at","market_time","market.timestamp","inputs.market_at","snapshot.market_at"])),decisionAt=parseTs(first(r,["decision_at","decided_at","decision.timestamp","trace.decision_at"])),availableAt=parseTs(first(r,["available_at","recorded_at","trace.available_at"]));
    const strategyBuild=first(r,["strategy_build","strategy_version","strategy.build","trace.strategy_build"]),policyBuild=first(r,["policy_build","policy_version","policy.build","trace.policy_build"]),direction=finite(first(r,["direction","direction_score","inputs.direction","inputs.direction_score","signal.direction"])),confidence=finite(first(r,["confidence","confidence_score","inputs.confidence","inputs.confidence_score","oracle.confidence"])),btc24=finite(first(r,["btc_24h_pct","btc24h","inputs.btc_24h_pct","market.btc_24h_pct"])),expectedMove=finite(first(r,["expected_move_pct","oracle_amplitude","inputs.expected_move_pct","inputs.oracle_amplitude"])),dataReady=first(r,["data_ready","inputs.data_ready","trace.data_ready"]),costGate=finite(first(r,["cost_gate_pct","cost_gate_threshold_pct","inputs.cost_gate_pct","inputs.cost_gate_threshold_pct"]));
    const missing=[];if(!id)missing.push("id");if(!asset)missing.push("asset");if(!decision)missing.push("decision");if(marketAt===null)missing.push("market_at");if(decisionAt===null)missing.push("decision_at");if(availableAt===null)missing.push("available_at");if(!strategyBuild)missing.push("strategy_build");if(!policyBuild)missing.push("policy_build");if(direction===null)missing.push("direction");if(confidence===null)missing.push("confidence");if(btc24===null)missing.push("btc_24h_pct");if(expectedMove===null)missing.push("expected_move_pct");if(dataReady!==true&&dataReady!==false)missing.push("data_ready");if(costGate===null)missing.push("cost_gate_pct");if(marketAt!==null&&decisionAt!==null&&marketAt>decisionAt)missing.push("market_after_decision");if(decisionAt!==null&&availableAt!==null&&decisionAt>availableAt)missing.push("decision_after_available");
    return Object.freeze({id:id?String(id):null,asset,decision,market_at:marketAt,decision_at:decisionAt,available_at:availableAt,strategy_build:strategyBuild?String(strategyBuild):null,policy_build:policyBuild?String(policyBuild):null,direction,confidence,btc_24h_pct:btc24,expected_move_pct:expectedMove,data_ready:dataReady,cost_gate_pct:costGate,certified:missing.length===0,missing:Object.freeze(missing)});
  }
  function t0Audit(){const rows=ledgerRows(),norm=rows.map(normalizeT0),cert=norm.filter(x=>x.certified),missing={};for(const n of norm)for(const k of n.missing)missing[k]=(missing[k]||0)+1;return Object.freeze({status:cert.length?"TRACEABLE_T0_AVAILABLE":"WAITING_T0_CAPTURE",ledger_rows:rows.length,traceable_rows:norm.filter(x=>!!x.id&&x.decision_at!==null).length,certified_rows:cert.length,certified:Object.freeze(cert),missing_field_counts:Object.freeze(missing),current_runtime_backfill:false,current_oracle_applied_to_past:false,future_outcomes_used:false});}
  function datasetFrom(t=t0Audit(),s=temporal()){
    const blockers=[];if(!s.certified)blockers.push("TEMPORAL_WINDOW_NOT_CERTIFIED");if(!t.certified_rows)blockers.push("NO_CERTIFIED_T0_DECISION");const joined=[];
    if(s.certified&&t.certified_rows){for(const d of t.certified){if(d.asset&&s.asset&&d.asset!==s.asset)continue;if(d.market_at<s.first_ms||d.market_at>s.last_ms)continue;joined.push(Object.freeze({decision:Object.freeze({...d}),market_window:Object.freeze({asset:s.asset,first_at:s.first_at,last_at:s.last_at,cadence_min:s.observed_cadence_min,points:s.window_points}),future_outcome:null,economic_result:null}));}}
    if(t.certified_rows&&joined.length===0)blockers.push("NO_T0_DECISION_INSIDE_CERTIFIED_WINDOW");const ready=blockers.length===0&&joined.length>0;
    return Object.freeze({schema:"agent_crypto_g3_replay_dataset_certification_v1",build:BUILD,status:ready?"READY_FOR_DECISION_REPLAY":"NOT_READY",ready,joined:Object.freeze(joined),joined_count:joined.length,blockers:Object.freeze(blockers),future_outcomes_attached:false,economic_backtest:false,execution_cost_model:false,lookahead:false,paper_only:true,g3:"PENDING",g9:"LOCKED"});
  }
  function snapshot(){const temporalState=temporal(),t0=t0Audit(),dataset=datasetFrom(t0,temporalState);return Object.freeze({schema:"agent_crypto_g3_cascade_checkpoint_v3",build:BUILD,stage:"REPLAY_DATASET_CERTIFICATION",temporal:temporalState,t0,dataset,paper_only:true,g3:"PENDING",g9:"LOCKED",real_order:false,profitability_claim:false});}
  function self_test(){const full=normalizeT0({cycle_id:"A-CYCLE-X",asset:"BTC",decision:"NO_TRADE",market_at:"2026-09-16T10:00:00Z",decision_at:"2026-09-16T10:00:01Z",available_at:"2026-09-16T10:00:02Z",strategy_build:"40.6.56",policy_build:"40.6.56",direction:-16,confidence:94,btc_24h_pct:-.8,expected_move_pct:.44,data_ready:true,cost_gate_pct:.8});const incomplete=normalizeT0({cycle_id:"X"});return {build:BUILD,pass:full.certified===true&&full.decision==="NO_TRADE"&&incomplete.certified===false,checks:{complete_no_trade_certifies:full.certified===true,incomplete_fails_closed:incomplete.certified===false}};}
  function render(){const d=snapshot();if(typeof document==="undefined")return d;const dossier=document.getElementById(DOSSIER_ID);if(!dossier)return d;let root=document.getElementById(ROOT_ID);if(!root){root=document.createElement("section");root.id=ROOT_ID;dossier.appendChild(root);}root.style.cssText="margin-top:9px;padding:8px;border:1px solid rgba(120,210,255,.25);border-radius:8px;background:rgba(6,24,34,.30)";const e=x=>String(x??"INCONNU").replace(/[&<>]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;"}[m]));root.innerHTML=`<div style="font-size:8px;font-weight:950;letter-spacing:.08em;color:#85e8ff">G3 · CASCADE CHECKPOINT · ${BUILD}</div><div style="font-size:8px;color:#9bb8c3;margin-top:3px">Fenêtre temporelle + T0 historique + jointure immuable · outcomes/coûts encore séparés.</div><div style="display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:5px;margin-top:7px"><div><small>24H</small><b style="display:block">${e(d.temporal.status)}</b></div><div><small>LEDGER</small><b style="display:block">${e(d.t0.ledger_rows)}</b></div><div><small>T0 CERT.</small><b style="display:block">${e(d.t0.certified_rows)}</b></div><div><small>JOINTES</small><b style="display:block">${e(d.dataset.joined_count)}</b></div><div><small>DATASET</small><b style="display:block">${e(d.dataset.status)}</b></div><div><small>G3</small><b style="display:block">PENDING</b></div></div><div style="margin-top:6px;font-size:8px;color:#9bb8c3">${d.dataset.ready?"Dataset de décision prêt pour replay logique; outcome post-horizon et replay économique restent requis.":e(d.dataset.blockers.join(" · ")||"WAITING_EVIDENCE")}</div>`;return d;}
  globalThis.AgentCryptoStrategyAG3CascadeCheckpoint=Object.freeze({build:BUILD,stage:"REPLAY_DATASET_CERTIFICATION",snapshot,render,self_test,normalize_t0:normalizeT0,dataset_from:datasetFrom,recurring_timer:false,observer:false,storage_write:false,network:false,real_order:false,paper_only:true,g3:"PENDING",g9:"LOCKED"});
})();
