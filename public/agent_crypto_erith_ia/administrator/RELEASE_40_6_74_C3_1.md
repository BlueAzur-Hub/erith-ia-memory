# Agent-Crypto Administrator — 40.6.74 Candidate 3.1

## Aether Watch V2 — Firefox Floating Fit Corrective

Parent field state: **40.6.74 C3** on the validated **40.6.73 Administrator** runtime / **Market Core 38.15.11**.

The Firefox field screenshots show that the immersive / maximized Aether composition is now close to target, but the ~1080 px floating stage still exposes cascade conflicts left by C2/C2.1 and the historical 40.6.60 density rules.

### What the field screenshots proved

- Fullscreen Aether is visually healthy enough to freeze for this pass; C3.1 does not change the >1180 px composition.
- In the floating stage, the older C2 `max-width:1100px` fallback still hides market assets 4 and 5, so XRP / SOL disappear even though C3 intended five assets until a genuinely narrow stage.
- The V2 minimum type sizes remain too large for Sources / Oracle / Weather at ~1080 px, producing `12 QUA…`, `CONTINU…` and unnecessary clipping.
- Atlas still prints transient machine prose across the baked world-map area. The score is useful; the machine string is not first-glance information at floating width.
- The top operational strip spends scarce width on the full date while status and time are being truncated.
- The first screenshot's `N/D` system values are a transient warm-up state: later captures in the same session show valid CPU / RAM / GPU values. C3.1 therefore does not create a second fetch/network owner or force a new refresh cadence.

### C3.1 correction

C3.1 **replaces** the C3 refinement stylesheet at runtime. It does not add a fourth active CSS layer.

Loaded refinement:

`aether-v2-refinement-406074-c31.css`

At `aether49 <= 1180px` only:

- restore all five Market assets; keep the three-asset emergency fallback only below 900 px;
- reduce Sources / Oracle / Weather metric floors to match the proportional backplate geometry;
- hide Atlas machine prose and Atlas transient header state while preserving score + report fact;
- hide Oracle transient header state in floating mode while preserving the four facts;
- make the system banner prioritize operational status + clock; hide the date only in floating mode;
- keep two Recent Event rows with narrower metadata columns;
- reduce Market breadth typography enough to preserve `hausses / baisses / stables` rather than clipping the third value;
- remove convergence/divergence footer prose in floating mode so verdicts own the capsules;
- leave the fullscreen composition unchanged.

### Protected

- global visible Administrator build remains **40.6.73** until Firefox field PASS;
- Market Core **38.15.11** unchanged;
- Administrator Window Manager unchanged;
- V2 backplate binary unchanged;
- R5 active Crypto fiche foreground behavior preserved;
- Aether data/runtime owners unchanged;
- no new timer;
- no MutationObserver;
- no storage owner;
- no network owner;
- no image generation or image modification.

### Field acceptance

1. Reload Administrator after Pages deploy.
2. Open Aether in the same floating geometry as the screenshots (~1080 px stage).
3. Confirm Market shows **BTC / ETH / BNB / XRP / SOL**.
4. Confirm Sources and Oracle no longer clip primary values (`12 QUALIFIÉS`, `CONTINUATION`, `84/100`) merely because of C2 minimum font floors.
5. Confirm Atlas shows the score cleanly without a machine-state string crossing the baked map.
6. Confirm the top-right strip prioritizes system status + time without a chopped date.
7. Maximize Aether and confirm the C3 fullscreen composition is unchanged.

If this receives field PASS, the next operation is the already agreed consolidation: reread the complete Aether zone, merge the validated V2 spatial/responsive/refinement contracts into one clean canonical **40.6.74**, remove redundant candidate layers, then promote build truth.
