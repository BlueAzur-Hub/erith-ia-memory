# Agent-Crypto @erith.IA — 40.6.4

## CHRONOS TRUE CENTER · VERSION SAFE REFRESH LOCK

Parent runtime canonique : **40.6.2**  
Candidat absorbé : **40.6.3 · Chronos True Center**  
Market Core : **38.15.11 — protected**

### Dette soldée

Le bouton de version visible conserve son propriétaire unique `js/version-truth.js`, mais retrouve le geste historique **Version Awareness / Safe Refresh** perdu lors de 40.5.19.

Au clic :

`build.json no-store → vérité publiée → URL de document unique → rechargement Firefox`

La navigation ajoute uniquement `ac-refresh=<build>-<timestamp>` à l'URL courante. Les paramètres existants, dont `?view=intermediate`, sont conservés.

### 40.6.3 absorbée

La correction Chronos candidate est incluse : le groupe `date · heure · jour planétaire · heure planétaire` est centré comme un objet intrinsèque unique, sur une seule ligne desktop, sans toucher aux calculs ChronosXP.

### Protections

- aucun changement Market Core 38.15.11 ;
- aucun changement Graphique / Atlas / Oracle / News / Aether ;
- aucun changement Strategy A / Paper ;
- aucun ordre réel ;
- aucun nouveau timer récurrent ;
- aucun MutationObserver ;
- aucun effacement cookies / localStorage / IndexedDB ;
- aucun reset Window Manager ;
- l'ancien `atlasVersionControl` reste un sink caché, jamais un second propriétaire visible.

### Validation Firefox attendue

1. après upload, **un dernier Ctrl+F5** peut être nécessaire pour charger ce nouveau contrat ;
2. badge : `Build 40.6.4 · Administrator` ;
3. Chronos : ligne complète et centrée ;
4. cliquer ensuite sur le badge de version ;
5. la page doit réellement se recharger, sans Ctrl+F5 ;
6. après rechargement, les données locales, fenêtres et préférences doivent être conservées.

Si ces six preuves passent, le bouton de version est de nouveau fonctionnel et 40.6.4 peut servir de checkpoint de reprise.
