# Agent-Crypto Administrator — 40.6.74 Candidate 4

## Aether Watch V2 — Reframe Rewrite

Parent field state: **40.6.74 C3.1** on the validated **40.6.73 Administrator** runtime / **Market Core 38.15.11**.

C3.1 was rejected in Firefox field review. The error was conceptual: it continued treating the symptom as a density/truncation problem and kept shrinking or hiding information. The four field captures instead show a framing problem across floating, maximized-browser, F11/workspace and F11-maximized states.

C4 therefore rewrites the candidate presentation path instead of appending another corrective tail.

### Single candidate owner

C4 loads only:

`aether-v2-reframe-406074-c4.css`

Before attaching C4, the loader removes the active candidate links for:

- C2 spatial (`aetherReadability406074`);
- C2.1 stabilization (`aetherV2Stabilization406074C21`);
- C3 refinement (`aetherV2Refinement406074C3`);
- C3.1 refinement (`aetherV2Refinement406074C31`).

The historical validated 40.6.73 Aether stylesheet remains the fallback when the V2 backplate cannot decode.

### Recadrage

The V2 PNG remains byte-for-byte unchanged.

C4:

- keeps one measured 1672×941 radial geometry;
- reframes live DOM blocks inside the larger painted capsules instead of shrinking them against the top edge;
- restores useful Atlas signal/resident information;
- keeps Sources and Oracle as readable 2×2 blocks;
- keeps BTC / ETH / BNB / XRP / SOL visible as one five-asset unit;
- retains three Recent Event rows at large size and two at medium floating size;
- keeps the central attention nucleus aligned below the baked AETHER identity;
- uses one responsive hierarchy instead of conflicting C2/C2.1/C3/C3.1 rules.

### Maximized / F11 framing

The historical Administrator Window Manager still owns open/close, maximize/restore, drag, persistence and z-order.

C4 adds only an Aether-local presentation reframe after the canonical maximize click:

- largest complete **16:9** Aether rectangle inside the current viewport;
- centered with a 12 px safety margin;
- no stretching of the 1672×941 artwork;
- browser resize / F11 re-evaluates the same 16:9 frame;
- restoring the window returns to the Window Manager's canonical saved geometry.

No Window Manager core file is modified.

### Protected

- visible/global Administrator build remains **40.6.73** until Christophe field-validates;
- Market Core **38.15.11** unchanged;
- V2 PNG unchanged;
- Graphique unchanged;
- Lecture Technique unchanged;
- Atlas / Oracle / Sources data owners unchanged;
- Aether network/system/weather owners unchanged;
- R5 active Crypto fiche foreground behavior preserved;
- no timer;
- no MutationObserver;
- no storage owner;
- no new network owner;
- no trading action.

### Field acceptance — reproduce the four captures

1. Firefox normal: open Aether over the workspace at the current floating size.
2. Click Aether **Agrandir** without F11: the Aether frame must remain a centered 16:9 scene rather than a 97vw × 97vh plate with internal side letterboxing.
3. Return to the workspace/F11 state: the radial scene must keep the same crop and card alignment.
4. F11 + Aether Agrandir: the scene must use the largest complete 16:9 rectangle with no distortion.
5. In every state verify:
   - five market assets remain visible;
   - Sources and Oracle remain 2×2;
   - Atlas retains score, signal, reports and resident lane where space permits;
   - no text is written over the decorative map merely to fill empty space;
   - Aether restore returns to the previous native window geometry.

If C4 passes these four framing states, consolidate the validated V2 presentation into canonical **40.6.74** and remove dormant candidate files in one cleanup pass.
