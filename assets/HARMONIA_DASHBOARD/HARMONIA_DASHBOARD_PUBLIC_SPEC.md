# HARMONIA_DASHBOARD_PUBLIC_SPEC

Status: public dashboard deployment package.

Rule:

Private repository = deep memory, archives, internal work.
Public repository = interface, dashboard, usable access point.

The dashboard uses local JSON snapshots stored in assets/HARMONIA_DASHBOARD/data/.
It does not fetch private files.
